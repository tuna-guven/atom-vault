[package]
name = "p2p-sync"
version = "0.1.0"
edition = "2024"

[dependencies]
# Transport & Multiplexing
rusqlite = { version = ">=0.36, <0.40", features = ["bundled"] }
arti-client = { version = "0.43.0", features = ["onion-service-client"] }
tor-keymgr = { version = "0.43.0", features = ["ephemeral-keystore", "keymgr"] }
tor-hsservice = "0.43.0"
tokio-socks = "0.5"
tokio = { version = "1.0", features = ["full"]}
tokio-util = { version = "0.7", features = ["compat"] }
yamux = "0.14"
futures = "0.3"

# Handshake & Cryptography
snow = "0.10"
ed25519-dalek = "2.1"
hkdf = "0.13"
sha2 = "0.11"

# Tor Integration
data-encoding = "2.11"
sha3 = "0.12"
tor-rtcompat = "0.43.0"

# Synchronization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
bincode = "1.3"
zeroize = "1.9"
libc = "0.2.186"
